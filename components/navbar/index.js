import React from 'react';

import './styles.css'

import Container from '../container'

const Navbar = props => (
    <nav className="navbar">
        <Container>
            Choque de Cultura
        </Container>
    </nav>
);


export default Navbar;
